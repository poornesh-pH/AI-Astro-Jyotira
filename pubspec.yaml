name: jyotira
description: "Jyotira — Vedic astrology & kundli with grounded, deterministic AI."
publish_to: "none"
version: 1.0.0+1
 
environment:
  sdk: ">=3.4.0 <4.0.0"
 
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  sweph: ^2.10.3
  purchases_flutter: ^8.1.0
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  intl: ^0.20.2
  http: ^1.2.1
  shared_preferences: ^2.2.3
  timezone: ^0.9.4
 
dev_dependencies:
  flutter_lints: ^4.0.0
 
flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/data/cities_in.json
    - assets/ephe/
